

## Plan: Page "Under Construction"

Replace the current Index page content with a simple, elegant "under construction" page while preserving the current code for future use.

### Steps

1. **Save current page**: Rename/copy `src/pages/Index.tsx` to `src/pages/IndexFull.tsx` to preserve all current content for future use.

2. **Replace Index.tsx**: Create a clean "under construction" page with:
   - DrinkBooking logo centered
   - "Site under construction" message in Portuguese ("Site em Construção")
   - A subtle animation
   - WhatsApp contact link
   - "Powered by 2my.ai" footer
   - Dark luxury theme matching current design

The full landing page will be safely stored in `IndexFull.tsx` and can be restored at any time by copying it back.

